# bible-mem

A project for strengthening Bible verse memory.

## Testing

To run coverage analysis, execute the following from the root "bible-mem"
directory:

`poetry run pytest --cov . --cov-report xml:cov.xml`
